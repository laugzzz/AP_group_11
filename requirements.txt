# -----------------------------------------------------------------------------
# Project dependencies � EMG TCP Signal Visualization Application
#
# Install with:
#   pip install -r requirements.txt
#
# Note: a full pinned version file with security hashes (SHA-256 checksums)
# can be generated with:
#   uv export --format requirements-txt --no-dev -o requirements.txt
# Hashes verify that each downloaded package has not been tampered with.
# For normal use, this simple version below is sufficient.
# -----------------------------------------------------------------------------

numpy>=2.4.6        # numerical arrays, buffer management (allow newer wheel for Python 3.14)
#numpy==2.2.6       # numerical arrays, buffer management (pinned for Python 3.13)
scipy==1.17.1       # bandpass filter (butter + filtfilt) and signal processing
PySide6==6.11.0     # Qt GUI framework (windows, buttons, layouts, signals)
vispy>=0.16.2       # GPU-accelerated real-time signal plotting (allow newer wheel)
matplotlib==3.10.8  # offline signal inspection plots
